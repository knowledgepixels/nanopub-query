# Notes on Swagger inclusion:

Files correspond to `dist` folder of this repo: https://github.com/swagger-api/swagger-ui

`LICENSE` file added.

Added this to `swagger-initializer.js`:

    queryConfigEnabled: true,